@using Web.Models
@model Web.Models.OrganizationHomepageViewModel
@using Microsoft.AspNet.Identity

@{
    ViewBag.Title = "Homepage";
}

<div class="panel panel-default text-center">
    <div class="panel-body" id="title">
        <h2><b>@ViewBag.Title</b></h2>
    </div>
</div>

<style>
    hr{
        display:none;
    }
    .footer1{
        display:none;
    }
    .icon{
        display:none;
    }
</style>
<link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">

<body style="overflow-y:hidden; overflow-x:hidden;">
    <div class="col-md-4 ">
        <div class="well" id="topCandidates" style="height: 490px;">
            <h2>Top Candidates</h2>
            <table class="table">
                <tbody>
                    @foreach (var candidate in Model.topCandidates)
                    {
                        <tr>

                            <h5>@Html.ActionLink(@candidate.CandidateName, "Profile", "Candidate", new { id = candidate.CandidateId }, null)</h5>
                            <p>
                                @if (@candidate.CandidateRating >= 1)
                                { <span class="fa fa-star checked"></span> }
                                else
                                { <span class="fa fa-star"></span> }
                                @if (@candidate.CandidateRating >= 2)
                                { <span class="fa fa-star checked"></span> }
                                else
                                { <span class="fa fa-star"></span> }
                                @if (@candidate.CandidateRating >= 3)
                                { <span class="fa fa-star checked"></span> }
                                else
                                { <span class="fa fa-star"></span> }
                                @if (@candidate.CandidateRating >= 4)
                                { <span class="fa fa-star checked"></span> }
                                else
                                { <span class="fa fa-star"></span> }
                                @if (@candidate.CandidateRating >= 5)
                                { <span class="fa fa-star checked"></span> }
                                else
                                { <span class="fa fa-star"></span> }
                            </p>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>


    <div class="col-md-8 ">
        <div class="well" id="offers" style="height: 490px;">

            <ul class="nav">
                @foreach (var ev in Model.orderedOffers)
                {

                    <li>
                        <div class="thumbnail" id="offer">
                            <div class="row">
                                <div class="col-md-offset-5">
                                    <h3>&emsp;@Html.ActionLink(@ev.Name, "Index", "Offer", new { id = ev.Id }, null)</h3>
                                </div>
                            </div>
                            <p>&emsp; <b>By: </b> @Html.ActionLink(@ev.Owner, "Profile", "Organization", new { id = ev.OwnerId }, null)</p>
                            <p>&emsp; <b>Location: </b> @ev.Location</p>
                            <p>&emsp; <b>Time: </b> @ev.DateTimeUTC</p>
                        </div>
                    </li>
                    <br />
                }
            </ul>

        </div>
    </div>
</body>


@using System.Security.Claims
@using Microsoft.AspNet.Identity;
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    <link href="~/Scripts/vendor/bootstrap/css/bootstrap.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">

    <!-- Custom styles for this template -->
    <link href="~/Scripts/one-page-wonder.css" rel="stylesheet">
    <style>
     .imageclass{
     display:block;
     position:absolute;
     background: url(../../fonts/logo.png) no-repeat top left;
    background-repeat:no-repeat;
    background-size: 100% 100%;
    width:140px;
    height:120px;
    left:0;
    float:left;
    left:0;
     /*display: block;
     visibility:visible;
     padding-bottom:100px;
     padding-top:10px;
     width: 50px;
     float:left;
     height: 50px;*/
     text-indent: -9999px; /* hides the link text  
     /*position:relative;*/
       }
    </style>
</head>

<body>
    <header class="fixed-top navbar navbar-expand-lg navbar-dark bg-eggplant" style="height:60px;border-radius:0;width:99%; margin-left:0.5%;">
        <div class="blackHeader bg-dark">
            <div class="leftContainer">
                @*<img class="leftMenu" src="https://www.w3schools.com/images/w3schools_green.jpg" style="height:40px;width:40px"
                *@
                @*<img class="leftMenu" src="~/fonts/logo.png" style="height:105px;width:120px">@Html.ActionLink("Vocial", "Index", "Home", new { area = "" }, new { @style = "color:white;" })</img>*@
               


                @Html.ActionLink("Home", "Index", "Home")
                @if (User.Identity.IsAuthenticated)
                {
                    if (User.IsInRole("Organization"))
                    {
                        <h4 class="leftMenu" style="color:white">@Html.ActionLink("Home", "Homepage", "Organization", null, new { @style = "color:white; margin-left:50px;" })</h4>
                        <h4 class="leftMenu" style="color:white">@Html.ActionLink("Profile", "Profile", "Organization", new { id = User.Identity.GetUserId() }, new { @style = "color:white;" })</h4>
                    }
                    else if (User.IsInRole("Candidate"))
                    {
                        <h4 class="leftMenu" style="color:white">@Html.ActionLink("Home", "Index", "Candidate", null, new { @style = "color:white; margin-left:50px;" })</h4>
                        <h4 class="leftMenu" style="color:white">@Html.ActionLink("Profile", "Profile", "Candidate", new { id = User.Identity.GetUserId() }, new { @style = "color:white;" })</h4>
                    }
                }
                else
                {

                }
            </div>
            <div class="rightContainer">
                @Html.Partial("_LoginPartial")
            </div>
        </div>
</header>
    <div class="container body-content bg-sand">
        @RenderBody()

    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)

    <!-- Footer -->
    <footer id="footer1" style="margin-bottom:-20px;display: block" class="bg-dark footer1">

       
    </footer>
 
</body>
</html>



<style type="text/css">
    body {
        overflow-y: hidden;
        background-color: #94618e;
    }

    #title {
        background-color: #f8eee7;
        background-repeat: no-repeat;
        background-position: left center;
    }

    #topCandidates {
        background-color: #f8eee7;
        overflow-x: hidden;
        overflow-y: auto;
        margin-bottom: 7px;
        height: 550px;
        width: 350px;
    }

    #topOrganizations {
        background-color: #f8eee7;
        overflow-x: hidden;
        overflow-y: auto;
        margin-bottom: 7px;
        height: 550px;
        width: 350px;
    }

    #offers {
        background-color: #94618e;
        overflow-x: hidden;
        overflow-y: auto;
        height: 550px;
        width: 720px;
    }

    #offer {
        background-color: #f8eee7;
        width: 665px;
    }

    .checked {
        color: orange;
    }

    .nav-item {
        margin-left: 5%;
    }

    #nav-title-container {
        width: 50px;
        height: 100px;
        padding: 0;
        border-color: white;
        border-width: 2px;
    }

    .leftContainer {
        position: absolute;
        float: left;
        width: 80%;
        height: 60px;
        display: flex;
        flex-wrap: wrap;
        align-items: center;

        background-color: transparent;
        color: white;

        padding-left: 5%;
    }

    .rightContainer {
        position: relative;
        margin-right:0px;
        float: right;
        width: 20%;
        height: 60px;

        display: flex;
        flex-wrap: wrap;
        align-items: center;

        background-color: transparent;
        color: white;
    }

    .blackHeader {
        height: 60px;
        width: 100%;
    }

    .leftMenu {
        position: relative;
        margin-left: 20px;
        color: white;
    }

    .footerBottomContainer {
        position:relative;
        width: 100%;
        height: 25px;

        align-items: center;
        justify-content: center;

        background-color: transparent;
        color: white;
        top: 5px;
    }

    .footerSocialContainer {
        position: relative;
        margin-top:10px;
        float: left;
        width: 100%;
        height: 25px;

        display: flex;
        flex-wrap: wrap;
        align-items: center;
        justify-content: center;

        background-color: transparent;
        color: white;
    }

    .fa {
        padding: 4px;
        font-size: 16px;
        width: 25px;
        height: 25px;
        text-align: center;
        text-decoration: none;
        margin-left:5px;
        margin-right:5px;
        border-radius: 50%;
    }

    .fa:hover {
        opacity: 0.6;
    }

    .fa-facebook {
        background: #3B5998;
        color: white;
    }

    .fa-twitter {
        background: #55ACEE;
        color: white;
    }

    .fa-linkedin {
      background: #007bb5;
      color: white;
    }

    .fa-youtube {
      background: #bb0000;
      color: white;
    }

    .fa-instagram {
      background: #125688;
      color: white;
    }
    #landing{
        overflow-y:scroll;
    }
</style>